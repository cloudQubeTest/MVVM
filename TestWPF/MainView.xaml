<Window x:Class="TestWPF.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:meds="clr-namespace:TestWPF.Meds"
        xmlns:contact="clr-namespace:TestWPF.Contact"
        xmlns:risks="clr-namespace:TestWPF.Risks"
        xmlns:local="clr-namespace:TestWPF"
        mc:Ignorable="d"
        Title="MainView" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <DataGrid x:Name="dataGrid" Grid.RowSpan="2"/>
        <Rectangle Grid.Column="1" Fill="#FFF4F4F5" Stroke="Black"/>
        <TabControl x:Name="tabControl" Grid.Column="1" Grid.Row="1">
            <TabItem x:Name="Contact" Header="_Contact Info">
                <contact:ContactView/>
            </TabItem>
            <TabItem x:Name="Med" Header="_Medications">
                <meds:MedsView/>
                <!--<Grid Background="#FFE5E5E5"/>-->
            </TabItem>
            <TabItem x:Name="Risk" Header="_Risk Factors">
                <risks:RisksView/>
            </TabItem>
        </TabControl>

    </Grid>
</Window>
